{
  "name": "worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker + Supabase backend (free tier) that issues Ed25519-signed entitlement tokens verified by the Rust client (`crates/tbx-entitlements`), and also serves the tiny account UI (login / signup / pricing / manage subscription / cancel) and verifies emails via the Gmail API.",
  "main": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "keywords": [],
  "type": "module",
  "dependencies": {
    "@noble/curves": "^1.6.0",
    "@noble/hashes": "^1.5.0",
    "@supabase/supabase-js": "^2.45.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241000.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "wrangler": "^3.80.0"
  }
}
